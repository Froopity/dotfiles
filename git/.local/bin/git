#!/usr/bin/env bash
# Shadows /usr/bin/git on PATH. Under /mnt/c/* paths, Linux git is
# painfully slow, so use git.exe there.
case "$PWD" in
  /mnt/c/*) exec git.exe "$@" ;;
  *) exec /usr/bin/git "$@" ;;
esac
